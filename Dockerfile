# 1. Imagem base com Node.js
FROM node:18-slim

# 2. Instala dependências do sistema e o Google Chrome para o Puppeteer
RUN apt-get update && apt-get install -y \
    ca-certificates \
    fonts-liberation \
    libasound2 \
    libatk-bridge2.0-0 \
    libatk1.0-0 \
    libc6 \
    libcairo2 \
    libcups2 \
    libdbus-1-3 \
    libexpat1 \
    libfontconfig1 \
    libgbm1 \
    libgcc1 \
    libgdk-pixbuf2.0-0 \
    libglib2.0-0 \
    libgtk-3-0 \
    libnspr4 \
    libnss3 \
    libpango-1.0-0 \
    libpangocairo-1.0-0 \
    libstdc++6 \
    libx11-6 \
    libx11-xcb1 \
    libxcb1 \
    libxcomposite1 \
    libxcursor1 \
    libxdamage1 \
    libxext6 \
    libxfixes3 \
    libxi6 \
    libxrandr2 \
    libxrender1 \
    libxss1 \
    libxtst6 \
    lsb-release \
    wget \
    xdg-utils \
    --no-install-recommends \
    && rm -rf /var/lib/apt/lists/*

# 3. Define diretório de trabalho
WORKDIR /app

# 4. Copia arquivos de dependência
COPY backend/package*.json ./

# 5. Instala dependências
RUN npm install

# 6. Copia o código do backend
COPY backend/ ./

# 7. Expõe a porta do servidor
EXPOSE 3001

# 8. Comando para iniciar o robô
CMD ["node", "server.js"]
